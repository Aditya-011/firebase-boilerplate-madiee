/* Just copy and paste the respective fields in your package.json file */

{
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start",
    "dev": "node rundev.js",
    "build:dev": "react-scripts build",
    "build": "cross-env REACT_APP_ENV=production react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "devDependencies": {
    "cross-env": ""
  }
}
